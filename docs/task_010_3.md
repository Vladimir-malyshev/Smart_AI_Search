Архитектурное обновление: Разделение моделей для разных узлов системы, добавление retry-логики и защита контекста.

Пожалуйста, выполни следующие задачи, изменяя только файлы app/modules/snippet_evaluator.py, app/modules/ai_judge.py и app/core/llm.py:

1. Строгая привязка Gemma для оценщика (app/modules/snippet_evaluator.py)
Убедись, что переменная DEFAULT_MODEL для провайдера Gemini установлена строго в "gemma-3-27b-it". Использование любых версий flash в этом модуле запрещено.

2. Вынос модели Судьи в конфиг (app/modules/ai_judge.py)
В начале файла добавь чтение переменной окружения JUDGE_LLM_MODEL. Установи значение по умолчанию "gemini-3.1-flash-lite".
Обнови вызов llm.generate_json (или generate_text) внутри функции judge, чтобы он использовал именно эту переменную (model_name=JUDGE_LLM_MODEL).

3. Локальное усечение контекста Судьи (app/modules/ai_judge.py)
Чтобы Судья работал быстро и не тратил лишние токены даже на Flash Lite, контекст нужно усекать перед отправкой в LLM.
Внутри функции judge перед формированием промпта добавь логику:

    Создай локальную строку/переменную для усеченного контекста.

    Проитерируйся по текстам статей из словаря context.

    Обрежь каждый текст до 6000 символов. Если текст был обрезан, добавь в его конец маркер \n\n...[УСЕЧЕНО ДЛЯ СУДЬИ].

    КРИТИЧЕСКИ ВАЖНО: Исходный словарь context, переданный в функцию, изменяться не должен! Полные тексты должны сохраниться для последующих этапов.

4. Обработка ошибки 429 (app/core/llm.py)
В провайдере Gemini добавь автоматический retry с экспоненциальным ожиданием при получении ошибки 429 (Resource Exhausted / Too Many Requests).
Используй библиотеку tenacity (например, retry, wait_exponential, stop_after_attempt(4)) или напиши надежный асинхронный цикл с asyncio.sleep.