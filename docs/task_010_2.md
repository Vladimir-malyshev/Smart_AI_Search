Нам нужны умные интеграционные тесты для execution_engine.py, которые адаптируются под выбранного провайдера. Важно: логика Redis никуда не исчезла, она используется легаси-провайдером SearXNG.

Пожалуйста, обнови файл тестов (например, tests/test_execution_engine.py):

    Динамический запуск: Тест должен читать os.environ.get("SEARCH_PROVIDER", "jina").

    Тест реального поиска (test_live_search_provider): >    - Вызывает execute_search("Тестовый запрос AI").

        Ждет реального ответа от сети.

        Проверяет, что вернулся непустой список объектов SearchSnippet, и что поля title, url и snippet заполнены.

    Тест оркестратора (test_execute_all_deduplication):

        Передает в execute_all список из двух похожих запросов.

        Проверяет склейку результатов и отсутствие дубликатов по полю url.

    Условный тест Redis (test_searxng_redis_logic):

        Напиши тест, который выполняется только если SEARCH_PROVIDER == "searxng".

        В этом тесте проверь (можно с моками), что движок корректно обращается к Redis за получением ноды перед выполнением запроса.

        Если включена Jina или Tavily (в будущем), этот тест должен просто пропускаться (pytest.mark.skipif).

Напиши чистый асинхронный код тестов. Сохрани всю легаси-логику тестирования Redis, но изолируй её так, чтобы она не роняла тесты при активной Jina.